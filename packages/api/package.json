{
  "name": "my-finances-api",
  "version": "0.0.1",
  "description": "Express based API",
  "main": "index.js",
  "scripts": {
    "create.superuser": "node src/tools/create-super-user-cli.js",
    "test": "tap --no-coverage --no-timeout --bail --reporter=spec test/**/*.test.js | pino-pretty --colorize --hideObject",
    "test.new.user": "tap --no-coverage --no-timeout --bail --reporter=spec test/**/new-user-journey.test.js | pino-pretty --colorize --hideObject",
    "test.only": "tap --no-coverage --only --no-timeout --bail --reporter=spec test/**/*.test.js",
    "test.coverage": "tap test/**/*.test.js --100"
  },
  "author": "mike.evans@numical.com",
  "license": "UNLICENCED",
  "dependencies": {
    "@google-cloud/firestore": "^4.9.8",
    "ajv": "^7.2.4",
    "ajv-keywords": "^4.0.1",
    "body-parser": "^1.19.0",
    "common-tags": "^1.8.0",
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "my-finances-common": "workspace:^0.0.1",
    "pino": "^6.11.2",
    "pino-http": "^5.5.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "inquirer": "^8.0.0",
    "pino-pretty": "^4.7.1",
    "prettier": "^2.2.1",
    "supertest": "^6.1.3",
    "tap": "^14.11.0"
  }
}