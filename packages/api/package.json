{
  "name": "my-finances-api",
  "version": "0.0.1",
  "description": "Express based API",
  "main": "index.js",
  "scripts": {
    "test": "tap --no-coverage test/**/*.test.js --no-timeout  --bail | pino-pretty --colorize --hideObject",
    "test.gory": "tap --no-coverage test/**/*.test.js --no-timeout  --bail",
    "test.only": "tap --no-coverage --only --no-timeout --bail test/**/*.test.js",
    "test.coverage": "tap test/**/*.test.js --100"
  },
  "author": "mike.evans@numical.com",
  "license": "UNLICENCED",
  "dependencies": {
    "@google-cloud/firestore": "^4.9.6",
    "ajv": "^7.1.1",
    "ajv-keywords": "^4.0.0",
    "body-parser": "^1.19.0",
    "common-tags": "^1.8.0",
    "cookie-parser": "^1.4.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "my-finances-common": "workspace:^0.0.1",
    "pino": "^6.11.1",
    "pino-http": "^5.5.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "pino-pretty": "^4.7.0",
    "prettier": "^2.2.1",
    "supertest": "^6.1.3",
    "tap": "^14.11.0"
  }
}